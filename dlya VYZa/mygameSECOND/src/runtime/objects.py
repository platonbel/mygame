import runtime

items = {
    'sprite_block': runtime.defaultGroup.Group(),
    'entity': runtime.defaultGroup.Group(),
    'secondary_GUI': runtime.defaultGroup.Group(),
    'general_GUI': runtime.defaultGroup.Group()
}
